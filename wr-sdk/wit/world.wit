package wruntime:sdk@0.1.0;

// Import-only world for SDK utilities. Does NOT export wasi:http/incoming-handler
// so that linking into client-style modules (which export `run`) does not conflict.
// Each module's own cargo-component metadata declares its specific world and exports.
world sdk {
    import wruntime:db/database@0.4.0;
    import wruntime:tracing/span;
    import wruntime:blobstore/store@0.1.0;
    import wruntime:llm/inference@0.1.0;
    import wasi:cli/stderr@0.2.6;
    import wasi:http/types@0.2.6;
    import wasi:http/outgoing-handler@0.2.6;
    import wasi:io/streams@0.2.6;
    import wasi:io/poll@0.2.6;
    import wasi:io/error@0.2.6;
    import wasi:clocks/monotonic-clock@0.2.6;
}
