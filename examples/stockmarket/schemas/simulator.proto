syntax = "proto3";
package stockmarket;

service SimulatorService {
  rpc Run (SimRunRequest) returns (SimRunResponse);
}

message SimRunRequest {
  uint32 num_traders       = 1; // 0 means default 1
  uint32 orders_per_trader = 2; // 0 means default 1
  uint32 num_symbols       = 3; // 0 means default 1
}

message SimRunResponse {
  int64  total_orders          = 1;
  int64  total_trades          = 2;
  int64  total_volume          = 3;
  bool   ledger_valid          = 4;
  string verification_details  = 5;
  uint32 errors                = 6;
}
