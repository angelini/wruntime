syntax = "proto3";
package ecommerce;

service ClientService {
  rpc Run (RunRequest) returns (RunResponse);
}

message RunRequest  { uint64 count = 1; } // 0 means default 100
message RunResponse { uint64 completed = 1; }
